-- Optional manual schema (JPA auto-creates tables with ddl-auto=update)
CREATE DATABASE IF NOT EXISTS blood_donor_db;
USE blood_donor_db;

-- Tables are managed by Hibernate; indexes are defined on entities.
